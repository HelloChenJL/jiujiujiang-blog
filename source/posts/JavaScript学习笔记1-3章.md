---
title: JavaScript学习笔记1-3章
date: 2026-02-06T05:07:40.081Z
categories:
  - 学习笔记
tags:
  - JavaScript
---
# JavaScript学习笔记1-3章

## 第1章 什么是JavaScript

JavaScript是一门用来与网页交互的脚本语言，包含一下三个组成部分。

1.  ECMAScript：由ECMA-262定义并提供核心功能。
    
2.  文档对象模型（DOM）：提供与网页内容交互的方法和接口。
    
3.  浏览器对象模型（BOM）：提供与浏览器交互的方法和接口。
    

## 第2章：在HTML中使用JavaScript

向HTML页面中插入JavaScript的主要方法，就是使用<script>元素。

为了避免浏览器在呈现页面时出现明显的延迟,现代Web应用程序一般都把全部JavaScript引用放在<body>元素中页面内容的后面。

## 第3章 基本概念

1.  严格模式  "use strict";ECMAScript5增加了严格模式（strict mode）的概念。严格模式是一种不同的JavaScript解析和执行模式，ECMAScript3的一些不规范写法在这种模式下会被处理，对于不安全的活动将抛出错误。要对整个脚本启用严格模式，在脚本开头加上这一行，也可以单独指定一个函数在严格模式下执行，只要把这个预处理指令放到函数体开头即可：
    
2.  语法，借鉴了C语言
    
    1.  区分大小写
        
    2.  标识符，指的是**变量**、**函数**、**属性**的名字
        
        1.  第一个字符必须是 字母、下划线、或美元符号
            
        2.  其他字符可以是字母、下划线、美元符号或数字
            
        3.  按照惯例，ECMAScript标识符使用驼峰大小写形式，即第一个单词的首字母小写，后面每个单词的首字母大写  
            
3.  严格模式
    
4.  语句
    
    1.  ECMAScript中的语句以分号结束，省略分号意味着由解析器确定语句在哪里结束。加分号有助于在某些情况下提升性能，因为解析器会尝试在合适的位置补上分号以纠正语法错误
        
5.  关键字和保留字
    
    1.  最好还是不要使用关键字和保留字作为标识符和属性名，以确保兼容过去和未来的ECMAScript版本
        
6.  变量声明（var关键字、let声明、const声明）
    
7.  作用域：var作用域是函数级，即函数内都是效的，let、const只在{}有效
    
8.  变量提升：var有， let和const没有
    
9.  可改变：var、let可以修改，const只要存储对象时可以修改对象属性的值
    
10.  可以重复声明：var可以重复声明，let、const不可重复声明
    
11.  let、const声明的全局变量，不会挂载到window对象上，var会总结：**不使用var，const优先，let次之**
    

数据类型

1.  基本数据类型
    
    1.  Undefined、Null、Boolean、Number、String、symbol、BigInt
        
    2.  typeof操作符，一般用于基本类型的检测，返回相应类型的字符串
        
        1.  “undefined”,"boolean","string","number","object","function"
            
        2.  typeof 的局限性，在于无法精确判断出 null、数组、对象、正则 的类型。
            
2.  引用类型
    
    1.  Object（Object，Array，Date，Function, RegExp）
        

字符串

1.  数值、布尔值、对象、字符串值本身 都有toString()方法，用于转为字符串
    
2.  undefined和null没有toString()方法，但可以使用String()方法
    
3.  toString()只能作用在变量身上，不能作用在常量上。常量可以使用String()转换称为字符串。（number.toString(radix)）传参数可以进行进制转换
    

Null类型

1.  表示空对象指针
    
2.  undefined派生自null，因此用==比较返回true(null==undefined)
    

浮点数，指的是数值中包含一个小数点，并且小数点后面至少有一位数字

NaN

1.  任何涉及NaN的操作都返回NaN
    
2.  NaN与任何值都不相等，包括NaN本身
    

数值转换

1.  有3个函数可以把非数值转换为数值
    
    1.  Number() 可以用于任何数据类型
        
    2.  parseInt() 专门用于字符串转数值，用于解析整数
        
    3.  parseFloat() 专门用于字符串转数值，用于解析浮点数
        

模板字面量

1.  使用\`\`(反引号)包裹，在反引号内换行
    

字符串插值

1.  在模板字面量中通过${变量}方式，将变量嵌入模板字面量，需要注意的是其原理是调用变量的toString方法。
    

标签函数\]

1.  标签函数不自动调用变量的toString方法
    

原始字符串

1.  通过String.raw标签函数可以获得一个字符串（转义字符不被转义，原样输出）  
      
    

symbol类型

1.  Symbol 类型是一种基本数据类型，它表示一种独一无二的值，可用于对象属性的命名。
    
2.  Symbol 类型的值是通过 Symbol 函数创建的，每个 Symbol 值都是独一无二的，它们之间不会相等，即使它们的描述符相同。
    
3.  Symbol 值可以作为对象的属性名使用，通过对象的 Symbol 属性访问，例如：  
    
4.  除了作为对象属性名，Symbol 还可以用于实现类似于枚举类型的功能。例如：
    

1.  Object类型
    
    1.  对象其实就是一组数据与功能的集合
        
    2.  使用Object()构造函数创建对象var obj = new Object(); // 同 obj = {}
        
    3.  每个对象实例都有下列属性和方法
        
        1.  constructor 指向用于创建当前对象的构造函数 。（对于上面例子而言，就是Object()）
            
        2.  hasOwnProperty(propertyName) 用于检查给定属性是否在当前对象实例中
            
        3.  isPrototypeOf(Object) 用于检查传入的对象是否是当前对象的原型
            
        4.  propertyIsEnumerable(propertyName) 用于检查给定的属性是否能够使用for-in来枚举
            
        5.  [toLocaleString()](https://blog.csdn.net/yigongzi/article/details/90140093)用来返回格式化对象后字符串
            
        6.  toString() 返回对象的字符串表示
            
        7.  valueOf()返回指定对象的原始值。
            
            1.  JavaScript的许多内置对象都重写了该函数，以实现更适合自身的功能需要。因此，不同类型对象的valueOf()方法的返回值和返回值类型均可能不同。对象返回值Array数组实例对象。Boolean布尔值。Date以毫秒数存储的时间值，从 UTC 1970 年 1 月 1 日午夜开始计算。Function函数本身。Number数字值。Object对象本身。这是默认设置。String字符串值。
                
2.  操作符相等操作符
    
    1.  一元操作符
        
        1.  一元加号 (+) – 将操作数转换为数字
            
        2.  一元减号 (-) – 将操作数转换为数字，然后取反。
            
        3.  ++ 前缀/后缀增量 (++) – 在其操作数上加一
            
        4.  \-- 前缀/后缀递减 (--) – 从其操作数中减一
            
        5.  前置与后置的区别是与其他数运算时，前置会先执行递增（减）再与其他数运算，后置会先与其他数运算再对自身执行递增(减)
            
    2.  位操作符
        
        1.  ECMAScript中的所有\*\*数值(默认)\*\*都以IEEE754 64位格式存储存储，但位操作并不能直接操作64位，只能操作32位，所以需要将64位转换位32位再进行操作，再将结果从32位还原到64位。32位还原到64位不会造成数据丢失对于程序员来说没必要关心，接下来说明64位转32位的规则
            
        2.  64位转32位总体来说，会导致小数部分丢失，超出32位能表示的整数最大范围 \[-2147483648, 2147483647\]丢失  
            注意点
            
            1.  首先，将浮点数的小数部分截断，保留它的整数部分
                
            2.  然后，将整数部分通过取模运算保留其在32位表示下的最低5位的值
                
            3.  js的位操作符是包括符号位的
                
            4.  特殊值NaN、Infinity在位操作符中都会被当成0处理
                
            5.  遇到非数值时，会使用Number函数转换，再进行位操作
                
            6.  存在无符号整数
                
            7.  位操作很快，因为是在底层表示上完成的
                
        3.  按位非（连同符号位全部取反）
            
        4.  按位或（比较操作数二进制（包括符号位），同0为0，否则为1）
            
        5.  按异或（比较操作数二进制（包括符号位），不同为1，否则为0）;
            
        6.  按位与（比较操作数二进制（包括符号位），同1为1，否则为0）
            
    3.  布尔操作符
        
        1.  与 (&&)
            
            1.  短路操作，即如果第一个操作数能决定结果，就不会对第二个操作数求值
                
        2.  或 (||)
            
            1.  短路操作，即如果第一个操作数能决定结果，就不会对第二个操作数求值
                
        3.  非 (!)
            
            1.  先使用Boolean()转成布尔值再取反
                
        4.  异或（^）
            
            1.  比较操作数二进制（包括符号位），不同为1，否则为0)
                
        5.  左移(<<)
            
            1.  符号位不移动，末位补零
                
        6.  有符号右移)(>>)
            
            1.  保留符号位，空位用符号位的值来填充
                
        7.  无符号右移(>>>)
            
            1.  连同符号位一起右移，空位补零
                
    4.  关系操作符
        
    5.  包括小于、小于等于、大于、大于等于，注意不包括等于。返回值为布尔，需要注意的是，在进行多个关系符运算时，布尔值被类型转换的问题
        
    6.  等于、不等于，会将操作数进行类型转换，再确定操作数是否相等操作数x操作数y结果布尔将布尔转换为数值再比较数值字符串字符串转数值再比较对象对象是否指向同一对象NaNundefined相等NaNNaN
        
    7.  全等、不全等，在不进行类型转换下的比较，注意undefined不全等nulll
        
3.  条件操作符(三元运算符)
    
    1.  let n = 2 > 1 ? 1 : 0;  
        console.log(n); //1
        
4.  赋值操作符
    
    1.  简单赋值 =
        
    2.  复合赋值 \*=、/=、+=、-=、<<=、>>=、>>>=   
        注意，复合赋值只是简写并不会提升性能
        
5.  逗号操作符
    
    1.  let n = 1 ,n = 2
        
    2.  逗号表达式（最后一项为整个表达式的值）let n = (1, 2); // 注意用括号包裹  
        console.log(n);//2
        

语句

1.  if
    
2.  while
    
3.  do while（循环至少会被执行一次，再进行判断是否循环）
    
4.  for（使用while循环做不到的，使用for循环同样做不到。就是说，for循环只是把与循环有关的代码集合在一个位置）
    
5.  for-in（得到key值）
    
6.  for-of（得到元素）  [for-in 和for-of的区别以及使用场景](https://alidocs.dingtalk.com/i/nodes/2Amq4vjg89gPonQYIaABxPMBV3kdP0wQ)
    
7.  with（将代码作用域设置为特定对象，with语句影响性能和难于调试不推荐使用））
    
8.  break 立即退出循环
    
9.  continue 退出当前单次循环
    
10.  switch（switch语句在比较每个条件时，会进行全等比较）
    

函数

1.  任何函数都可以在任何时候返回任何值
    
2.  未指定返回值的函数会返回一个undefined
    
3.  通过arguments访问参数，它是一个[类数组](https://blog.csdn.net/weixin_41835811/article/details/127443062)
    

